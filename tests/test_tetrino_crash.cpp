
#include <catch2/catch_test_macros.hpp>

#include "Tetrino.hpp"

TEST_CASE("test tetrino crash") {

    /* fra eksempel
     std::string firstname = "Per";
     std::string lastname = "Olavsen";

     Person p(firstname, lastname);

     CHECK(firstname == p.getfirstname());
     CHECK(lastname == p.getlastname());

#include <catch2/matchers/catch_matchers_floating_point.hpp>

#include "Vector3.hpp"

    TEST_CASE("test_vector3") {

        Vector3 a(1.5, 0, 2);
        Vector3 b(0.5, 1, -2);

        Vector3 c = a.add(b);

        CHECK_THAT(c.x, Catch::Matchers::WithinRel(2.f));
        CHECK_THAT(c.y, Catch::Matchers::WithinRel(1.f));
        CHECK_THAT(c.z, Catch::Matchers::WithinRel(0.f));

    }
     */
}